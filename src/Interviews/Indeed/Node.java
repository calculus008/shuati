package Interviews.Indeed;

public class Node {
    int level;
    boolean isControl;

    public Node(int level, boolean isControl) {
        this.level = level;
        this.isControl = isControl;
    }
}
